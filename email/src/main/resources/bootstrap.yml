server:
  port: 8085
spring:
  application:
    name: email
  profiles:
    active: register,rabbitmq,email
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: study
        file-extension: yml
